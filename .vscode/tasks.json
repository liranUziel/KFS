{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build.bat",
            "command": "./build.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "gcc build with shell"
        }
    ]
}